[tool.poetry]
name = "modeld-python-sdk"
version = "0.3.648"
description = "python client sdk for the modeld auto ml system."
authors = ["tsagi <tsagi@metaprov.com>"]
keywords = ["modeld", "automl"]
readme = "README.md"
license = "MIT"
homepage = "https://github.com/metaprov/modeld-api-sdk"
repository = "https://github.com/MattiooFR/package_name"
include = [
    "LICENSE",
]

[tool.poetry.dependencies]
python = "^3.8"
modeldapi = "^0.3.646"
grpc = "^1.0.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
